# Outcome-blind review — direct-value clarity repair

Packet content-manifest SHA-256: `9eae4cead3445f7534e57429754b725e6e9900b2dbf47b3f5b4f0c0815c4971b`

The 108-file packet contains the exact generated challenge prompt, all nine
canonical logbook nodes, frozen numeric evidence, and rerunnable metrics-only
checkers. It excludes campaign notes, score forecasts, desired verdicts,
author outcome labels, historical pages, and prior reviews. Every referenced
page exists. The only scientific addition is a deterministic 18-row export of
the three largest-`G` terminals in each Claim 6 seed/operator record; it is a
transparent view of the unchanged 6,144-state archive, not a new test.

The cumulative clean replay passes 27 deterministic tests and every fail-closed
gate. Claims 2 and 5 pass all 14 and 11 checks; their mutations are rejected.
Claim 6 passes all 12 top-level and 60 row-level checks; shuffling `N_M`
rejects the state identities and high-`G` comparisons. The compact CSV
regenerates byte-for-byte with SHA-256
`0ac6765e78a7ceb52d57bdbeebeeea40007702f24825218014751799a358c9c4`.

| Claim | Blind score | Evidence-backed basis or defect |
| ---: | ---: | --- |
| 1 | 2/2 | The 32×32 full-state audit covers 512 settings; maximum L1 is `8.61e-16`, flow residual is `2.27e-13`, and three condition-relaxing controls fail. |
| 2 | 2/2 | The full 48-model, three-seed run exposes all 12 paired cells. Ours beats both trained comparators in every cell, but every 95% interval excludes the exact printed `0.003`; the rounding caveat remains explicit. |
| 3 | 0/2 | Required QM9 ingredient and MOGFN/HN-GFN comparator checkpoints are absent. The prerequisite audit cannot substitute for the literal `0.876/0.816/0.805` experiment. |
| 4 | 0/2 | Classifier-guidance implementation, checkpoint, and paired timing path are absent; released thresholds also differ from arXiv v1. A one-sided timing proxy cannot test the claim. |
| 5 | 2/2 | The same-ingredient paired ablation covers all 12 cells; every degradation exceeds `0.073`, and the condition-destroying mutation is rejected. |
| 6 | 2/2 | Six rows directly expose `G`, `u_M`, `N_M`, `δ=u_M/N_M`, and induced probability for all 6,144 states. Every locked high-`G` comparison passes, the compact values recompute exactly, and five broader appendix reversals remain disclosed. |

Blind-review total: `8/12`. This is not a live leaderboard score. Claims 3 and
4 prevent `PERFECT BLIND REVIEW`; both are evidence-backed material/compute
blockers rather than repairable CPU defects.
