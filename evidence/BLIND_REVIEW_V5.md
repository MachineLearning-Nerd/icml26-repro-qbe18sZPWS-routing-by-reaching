# Outcome-blind review — retained-route consistency repair

Frozen packet: 120 files. Packet manifest SHA-256:
`6e5af80e28d0be084d32b566d6311851030bd30deb1ba611e6291340360051e4`.
Sorted packet-tree SHA-256:
`5e434bfa78d0d10fb1a75a29ecaa20118d4007832715cc0fe9ea63fb3469c951`.

The packet contains the exact generated prompt, all nine canonical nodes,
every retained legacy claim route, the repaired alias map, frozen numeric
evidence, and metrics-only rerun scripts. It excludes campaign notes, score
forecasts, prior reviews, and author outcome labels. The outcome-label scan is
clean. The exact current challenge validator passes on a clean stage containing
only the eight ordered sidebar pages.

The three scoped packet reruns pass. Claim 2 recomputes all 14 neural/archive
checks; Claim 5 recomputes all 11 paired-ablation checks; Claim 6 recomputes all
12 top-level and 60 row-level state checks. Mutating the Claim 2 paper-point
observations, removing Claim 5's ablation gap, or shuffling Claim 6's `N_M`
values is rejected.

| Claim | Blind score | Evidence-backed basis or defect |
| ---: | ---: | --- |
| 1 | 2/2 | The full 32×32 state audit covers 512 scalarizations; maximum terminal L1 is `8.61e-16`, maximum independent flow residual is `2.27e-13`, and three condition-relaxing controls fail. |
| 2 | 2/2 | The 48-model, three-seed neural run exposes all 12 paired cells. The proposed method beats both trained comparators in every cell, but every 95% interval excludes the exact printed `0.003`; the rounding limitation remains explicit. |
| 3 | 0/2 | Required trained QM9 GAP/SA ingredients and MOGFN/HN-GFN comparators are absent. The prerequisite audit cannot replace the literal `0.876/0.816/0.805` experiment. |
| 4 | 0/2 | Classifier-guidance implementation, checkpoint, and paired timing path are absent; released thresholds also differ from arXiv v1. A one-sided timing proxy cannot test the claim. |
| 5 | 2/2 | The same-ingredient neural ablation covers all 12 cells; every degradation exceeds `0.073`, and the condition-destroying mutation is rejected. |
| 6 | 2/2 | Six rows expose `G`, `u_M`, `N_M`, `δ=u_M/N_M`, and induced probability over all 6,144 states. Every locked high-region comparison passes, direct values recompute, and five broader appendix reversals remain disclosed. |

All retained numbered claim paths now have the correct official claim number.
The legacy Claim 2 and Claim 3 aliases resolve to their same-number canonical
pages, and the methods/negative-control routes explicitly prevent exact-tabular
Claim 1 evidence from being substituted for Claims 2 or 5.

Blind-review total: `8/12`. This is not a live leaderboard score. Claims 3 and
4 prevent `PERFECT BLIND REVIEW`; both are evidence-backed material/GPU
blockers rather than repairable two-hour CPU defects.
