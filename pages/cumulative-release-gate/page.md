# Cumulative release gate (legacy route)

The live judge scored HF revision
`8de344e3c4a45398170457c954d2b70dd62dab7c` at `4/12`: Claim 1 verified,
Claims 2 and 5 toy, and Claims 3, 4, and 6 inconclusive. Its feedback repeats
the retained exact-tabular/proxy route text rather than the canonical neural
and direct-state pages. The current repair therefore makes all retained claim
routes consistent; it does not change the frozen scientific outputs.

The cumulative verifier reruns the exact Claim 1 regression, independently
checks the 48-model Claim 2/5 archive, checks all 6,144 Claim 6 state records,
rejects the claim-specific mutations, and validates the Claim 3/4 blocker
inventory. Only the live judge can change the `4/12` baseline.
