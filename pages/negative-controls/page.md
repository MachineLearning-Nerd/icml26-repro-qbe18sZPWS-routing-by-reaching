# Negative controls (legacy route)

This retained route is an index, not standalone claim evidence.

- Claim 1 rejects omission of reaching probability, omission of partition
  factors, and the wrong composition temperature in the exact theorem audit.
- Claim 2 rejects a mutation that replaces neural observations with the paper
  point because it no longer matches the immutable 48-model archive.
- Claim 5 rejects replacing the neural no-reaching observations with the full
  policy values.
- Claim 6 rejects a deterministic statewise shuffle of `N_M`, which breaks
  `δ=u_M/N_M`, Equation 9, and the high-region comparisons.

The official claim pages link each mutation beside the matching source-scale
evidence. Exact-tabular Claim 1 values are not substitutes for Claims 2 or 5.
